---
import MySiteLayout from "../layouts/AboutNowLayout.astro";
import { recipes } from "../recipes";
---

<MySiteLayout frontmatter={{ title: "Recipes" }}>
  <ul>
    {
      Object.entries(recipes).map(([slug, recipe]) => (
        <li>
          <a href={`/recipes/${slug}`}>{recipe.title}</a>
        </li>
      ))
    }
  </ul>
</MySiteLayout>
